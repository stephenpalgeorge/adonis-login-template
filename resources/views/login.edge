@layout('layouts/main')
@set('title', 'Login')
@set('description', 'Enter your account credentials to gain access.')

@section('assets')
  <link rel="stylesheet" href="/styles/elements/forms.css">
  <link rel="stylesheet" href="/styles/components/button.css">
  <link rel="stylesheet" href="/styles/components/form-field.css">
  <link rel="stylesheet" href="/styles/components/form-links.css">
@end

@section('body')
  <main>
    <div>
      <h1 class="title">Login</h1>

      <form action="{{ route('auth.login') }}" method="post">
        @if(flashMessages.has('errors'))
          <p class="error error--message">{{ flashMessages.get('errors') }}</p>
        @end

        @!component('components/form-field', {
          id: 'username',
          type: 'text',
          name: 'username',
          label: 'Username:',
          placeholder: 'your-name',
        })

        @!component('components/form-field', {
          id: 'password',
          type: 'password',
          name: 'password',
          label: 'Password:',
        })

        @!component('components/button', {
          type: 'submit',
          label: 'Log in',
        })
      </form>

      @!component('components/form-links', {
        links: [
          { route: 'pages.register', label: 'Need an account? Sign up.' },
          { url: '/test', label: 'Forgotten your password? Click here.' },
        ]
      })
    </div>
  </main>
@end
